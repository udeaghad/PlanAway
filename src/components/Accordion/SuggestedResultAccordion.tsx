import React from 'react'
import theme from '../../theme/theme'

const SuggestedResultAccordion = () => {
  return (
    <div style={{height: "40px", width: "80%", marginTop: "0.5rem", backgroundColor: theme.palette.secondary.variant}}>
      SuggestedResultAccordion
    </div>
  )
}

export default SuggestedResultAccordion;